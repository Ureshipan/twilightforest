package twilightforest.client;

import net.fabricmc.api.ClientModInitializer;

public class TwilightForestClient implements ClientModInitializer {

	@Override
	public void onInitializeClient() {
		// TODO Phase 2: register client events (entity renderers, block renderers, HUD overlays)
		// TODO Phase 9: register custom model loaders (FRAPI), client extensions
	}
}
